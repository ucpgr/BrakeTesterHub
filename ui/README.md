# Wt UI assets

- Source SCSS for the Wt Bootstrap theme lives in `ui/bootstrap-theme.scss`.
- Compiled static assets served by Wt live in `ui/www`.

## Build

```bash
cd ui
npm install
npm run build:bootstrap-theme
```
